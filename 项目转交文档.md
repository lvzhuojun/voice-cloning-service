# 项目转交文档

## 1. 项目目标

本项目已从原先的 CosyVoice 方案迁移到 GPT-SoVITS v2。

当前目标链路是：

用户上传音频 -> 服务端执行 GPT-SoVITS 微调 -> 导出 `storage/models/{voice_id}/` -> 下游语音助手直接加载 `{voice_id}_gpt.ckpt` 和 `{voice_id}_sovits.pth`，无需再提供参考音频进行模型加载。

运行目录：

- 项目根目录：`D:\Lyuzhuojun\Project\Forsis\voice-cloning-service`
- GPT-SoVITS 子目录：`D:\Lyuzhuojun\Project\Forsis\voice-cloning-service\GPT-SoVITS`
- Conda 环境：`voice-cloning`
- Python：3.10
- Anaconda：`D:\Anaconda3`
- GPU：RTX 5060 Laptop
- CUDA / Torch：PyTorch 2.7.0 + cu128

## 2. 当前已完成内容

以下迁移工作已经完成，整体方向已经切到 GPT-SoVITS：

- `README.md` 已重写为 GPT-SoVITS 版本文档。
- `VOICEPACK_FORMAT.md` 已改为双文件模型格式说明，明确产物为 `.ckpt + .pth`。
- `IMPROVEMENTS.md` 已补充 CosyVoice -> GPT-SoVITS 迁移说明。
- `static/index.html` 已适配新 API，包括 GPT/SoVITS/ZIP 下载和训练阶段展示。
- `start_service.bat` 已去掉旧 CosyVoice 的 `PYTHONPATH`。
- `environment.yml` 已精简为 Python 3.10 + ffmpeg + pip。
- `requirements-pip.txt` 已清理 CosyVoice 依赖，并补入部分 GPT-SoVITS 所需依赖。
- `setup/clone_gptsovits.bat`、`setup/clone_gptsovits.sh` 已新增。
- `setup/download_models.py` 已重写，用于下载 GPT-SoVITS 所需预训练模型。
- `app/core/trainer.py` 已重写为 8 步 subprocess 训练流水线。
- GPT-SoVITS 文本处理补丁已打到以下文件，解决 `jieba_fast` 缺失时回退 `jieba`：
  - `GPT-SoVITS/GPT_SoVITS/text/tone_sandhi.py`
  - `GPT-SoVITS/GPT_SoVITS/text/chinese.py`
  - `GPT-SoVITS/GPT_SoVITS/text/chinese2.py`
- `app/api/train.py`、`app/api/voices.py`、`app/core/voice_manager.py` 当前结构已与新模型格式对齐。

## 3. 当前代码结构

关键目录如下：

```text
app/
  main.py
  config.py
  api/
    health.py
    train.py
    voices.py
  core/
    trainer.py
    tts_engine.py
    voice_manager.py

GPT-SoVITS/
  GPT_SoVITS/
    TTS_infer_pack/TTS.py
    configs/s1longer-v2.yaml
    configs/s2.json

storage/
  models/{voice_id}/
    {voice_id}_gpt.ckpt
    {voice_id}_sovits.pth
    metadata.json
    reference.wav
  pretrained_models/GPT-SoVITS/
```

## 4. 已打通的服务能力

### 4.1 FastAPI 入口

- `app/main.py`
- 启动时会：
  - 初始化日志
  - 创建 `storage/`、`storage/uploads/`、`storage/models/`、`data/samples/`
  - 检查 GPT-SoVITS 目录和预训练模型目录是否存在

### 4.2 训练 API

- `POST /api/train/from-upload`
- `POST /api/train/from-folder`
- `GET /api/train/{task_id}/status`

训练任务当前通过 FastAPI `BackgroundTasks` 异步执行，状态保存在内存 `_task_store` 中；这意味着服务重启后任务状态不会持久化。

### 4.3 模型管理 API

- `GET /api/voices`
- `GET /api/voices/{voice_id}`
- `DELETE /api/voices/{voice_id}`
- `GET /api/voices/{voice_id}/download/gpt`
- `GET /api/voices/{voice_id}/download/sovits`
- `GET /api/voices/{voice_id}/download/all`
- `POST /api/voices/{voice_id}/test`

### 4.4 模型产物格式

每个声音存放于：

```text
storage/models/{voice_id}/
  {voice_id}_gpt.ckpt
  {voice_id}_sovits.pth
  metadata.json
  reference.wav
```

其中：

- `gpt.ckpt` 是 GPT 文本到语义模型。
- `sovits.pth` 是 SoVITS 声码器生成模型。
- `reference.wav` 仍然保留，用于当前推理调用时作为参考音频输入。

## 5. trainer.py 当前状态

`app/core/trainer.py` 已按 GPT-SoVITS 官方脚本思路重写，核心流程为：

1. 音频切片
2. Whisper 转写
3. BERT 特征提取
4. HuBERT 特征提取
5. Semantic token 提取
6. GPT 训练
7. SoVITS 训练
8. 收集并重命名最终模型文件

当前实现特点：

- 通过 subprocess 调 GPT-SoVITS 自带脚本，而不是本地手写训练逻辑。
- 训练工作目录放在 GPT-SoVITS 的 `logs/` 体系下。
- 最终统一复制到 `storage/models/{voice_id}/`。
- 完成后由 `VoiceManager` 生成 `metadata.json`。

## 6. 最高优先级未完成项

### 6.1 必须重写 `app/core/tts_engine.py`

这是当前最重要的未完成工作。

现状：

- `app/core/tts_engine.py` 仍是旧的“手动加载 GPT/SoVITS 模块 + 自己拼推理链”的实现。
- 这个版本与当前训练产物和 GPT-SoVITS 官方推理入口不一致，风险很高。

目标改法：

- 改为使用 `GPT-SoVITS/GPT_SoVITS/TTS_infer_pack/TTS.py` 里的 `TTS` 类。
- 必须先把以下路径加入 `sys.path`：
  - `GPT-SoVITS`
  - `GPT-SoVITS/GPT_SoVITS`

已确认的调用方式：

```python
from TTS_infer_pack.TTS import TTS, TTS_Config

config = TTS_Config({
    "custom": {
        "device": "cuda",
        "is_half": True,
        "version": "v2",
        "t2s_weights_path": "/abs/path/{voice_id}_gpt.ckpt",
        "vits_weights_path": "/abs/path/{voice_id}_sovits.pth",
        "cnhuhbert_base_path": "/abs/path/chinese-hubert-base",
        "bert_base_path": "/abs/path/chinese-roberta-wwm-ext-large",
    }
})

tts = TTS(config)

inputs = {
    "text": "...",
    "text_lang": "zh",
    "ref_audio_path": ".../reference.wav",
    "prompt_text": "",
    "prompt_lang": "zh",
    "speed_factor": 1.0,
    "top_k": 15,
    "top_p": 1.0,
    "temperature": 1.0,
    "batch_size": 1,
}

sr, audio = next(tts.run(inputs))
```

重要注意事项：

- `TTS.__init__()` 会立即加载 BERT、HuBERT、GPT、SoVITS，成本很高。
- 必须按 `voice_id` 做实例缓存，因为每个声音的权重路径不同。
- `TTS.run()` 返回 generator，不是直接返回 wav。
- 当前环境里 `peft` 版本仍存在约束风险，已安装版本是 `0.18.1`，但计划依赖约束是 `<0.18.0`。

### 6.2 更新 `requirements-pip.txt`

当前文件里仍缺少一批实际上已经装过、且 GPT-SoVITS 运行依赖到的包。

应补入：

```text
peft>=0.13.0,<0.18.0
ffmpeg-python>=0.2.0
fast_langdetect>=0.2.0
split-lang>=0.3.0
rotary_embedding_torch>=0.5.0
x_transformers>=1.30.0
torchmetrics>=1.0.0,<=1.5.0
ctranslate2>=4.0.0
av>=11.0.0
```

### 6.3 跑通端到端验证

建议验证顺序：

1. 启动服务
2. 调训练接口
3. 确认模型文件产出
4. 调测试 TTS 接口

参考命令：

```bat
conda activate voice-cloning
cd /d D:\Lyuzhuojun\Project\Forsis\voice-cloning-service
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

训练测试数据：

- `data/samples/1wav/`
- 当前描述中有 23 个 wav，接口会截断到前 10 个文件

### 6.4 更新测试文件

`tests/` 里仍有旧结构残留，需要整理：

- `tests/test_voicepack_manager.py`
  - 基于旧 VoicePack / VoiceManager 结构，已废弃
- `tests/test_embedding_extractor.py`
  - 对应模块已删除，已废弃
- `tests/test_api.py`
  - 需要按新 API 路径和返回结构重写

## 7. 预训练模型下载要求

预训练模型目标目录：

`storage/pretrained_models/GPT-SoVITS/`

需要下载的内容：

- `pretrained_s1.ckpt`
- `pretrained_s2G.pth`
- `pretrained_s2D.pth`
- `chinese-hubert-base/`
- `chinese-roberta-wwm-ext-large/`

执行方式：

```bat
conda activate voice-cloning
python setup/download_models.py
```

预计体积约 3 到 4 GB。

## 8. 当前仓库实际状态

基于本地 `git status`，当前工作区实际看到的未提交改动是：

- `app/core/trainer.py` 有修改未提交

这和上一轮备注里的“`static/index.html` 有未提交修改、`start_service.bat` 是新文件”不完全一致，说明其中部分改动可能已经被提交，或当前工作区与上一轮描述不同。接手时应以实时 `git status` 为准，不要直接依赖旧备注。

## 9. 推荐接手顺序

建议严格按以下顺序继续：

1. 重写 `app/core/tts_engine.py`
2. 更新 `requirements-pip.txt`
3. 下载预训练模型
4. 跑 `/api/train/from-folder` 或 `/api/train/from-upload`
5. 跑 `/api/voices/{voice_id}/test`
6. 更新或删除旧测试
7. 最后统一提交当前所有变更

## 10. 接手人注意事项

- 当前训练任务状态存在内存里，服务一重启就丢。
- `reference.wav` 虽不是下游加载模型必需，但当前 TTS 推理仍依赖它。
- `tts_engine.py` 如果继续保留旧实现，极可能成为端到端链路失败点。
- `requirements-pip.txt` 若不补齐，后续在新环境复现时会出现缺包。
- GPT-SoVITS 本体目录已经 clone，但是否完整可用仍要结合预训练模型和依赖一起验证。

## 11. 给新对话的简版指令

可直接把下面这段发给新的执行对话：

```text
继续完成 voice-cloning-service 的 GPT-SoVITS 迁移。

最高优先级：
1. 重写 app/core/tts_engine.py，改用 GPT_SoVITS/TTS_infer_pack/TTS.py 的 TTS 类
2. 更新 requirements-pip.txt，补齐 GPT-SoVITS 运行依赖
3. 下载预训练模型并跑通一次训练 + TTS 端到端验证
4. 整理 tests/ 下已过时的测试文件
5. 最后检查 git status 并提交当前变更
```
